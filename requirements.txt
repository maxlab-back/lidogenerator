# Зависимости лидогенератора. Верхние границы — чтобы новая мажорная версия
# не сломала установку на машине, где никто не следит за обновлениями.
requests>=2.31,<3
PyYAML>=6.0,<7
beautifulsoup4>=4.12,<5
openpyxl>=3.1,<4
python-dotenv>=1.0,<2
anthropic>=1.4,<2
pydantic>=2.7,<3
dnspython>=2.6,<3

# Необязательное, но желательное: быстрый парсер HTML. Если колесо не соберётся
# (Alpine/musl, редкая архитектура) — лидогенератор сам перейдёт на html.parser из stdlib.
lxml>=4.9,<7
